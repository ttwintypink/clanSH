# slash_commands.py
# Ранее здесь были /add и /del для управления списком исключения opener.
# Теперь opener определяется с учётом ролей (см. config.py -> IGNORED_TICKET_OPENER_ROLE_IDS),
# поэтому команды управления исключениями больше не нужны.

# Файл оставлен пустым намеренно, чтобы import slash_commands в main.py не ломался.
